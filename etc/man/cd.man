CD.EXE - change directory
Usage: cd <path>

Changes the current working directory.

Examples:
  cd /rom
  cd ..
  cd /

Notes:
- Without arguments, resets to system root.
- Paths may be relative or absolute.